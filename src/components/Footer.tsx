'use client'
import React from 'react';
import { Box } from '@mui/material';

const Footer = () => {
  return (
    <Box sx={{ ml: 40, mt: 'auto' }}>
      <Box sx={{ mx: 'auto'}}>
        Footer

      </Box>
    </Box>
  )
}

export default Footer